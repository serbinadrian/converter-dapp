import React from "react";

const NotFound = (): React.ReactElement => {
    return (
        <React.Fragment>
            <h1>NotFound</h1>
        </React.Fragment>
    );
};

export default NotFound;
