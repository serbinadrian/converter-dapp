import React from "react"

const Transactions = (): React.ReactElement => {
    return (
        <React.Fragment>
            <h1>Transactions</h1>
        </React.Fragment>
    )
}

export default Transactions;