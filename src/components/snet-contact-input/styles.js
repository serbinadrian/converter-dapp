import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  field: {
    fontSize: 16,
    color: '#424242',
    borderRadius: 4
  },
  fieldContainer: {
    paddingBottom: 30
  }
});
