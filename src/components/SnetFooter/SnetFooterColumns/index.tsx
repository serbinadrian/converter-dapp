import React from "react";

const SnetFooterColumns = (): React.ReactElement => {
    return (
        <React.Fragment>
            <h1>columns</h1>
        </React.Fragment>
    );
};

export default SnetFooterColumns;